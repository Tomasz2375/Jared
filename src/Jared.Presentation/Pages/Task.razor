@page "/task"
@using Jared.Application.Dtos.TaskDto;
@using Jared.Presentation.Components
@using MediatR;
@inject IMediator Mediator

@if (Model is null)
{
    return;
}

<DataGrid
    TItem="TaskListDto"
    Items="Model" />
